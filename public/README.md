# Static Assets (Public)

This directory is for static assets that are served from the root path of the application. 
Files in this folder (like `logo.png` or `favicon.ico`) can be referenced directly via `/filename`.

- **Usage:** Static images, robots.txt, manifest files, and fonts.
- **Access:** Accessible via `https://your-domain.com/filename`.