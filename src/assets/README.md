# Source Assets

This directory is intended for assets that are imported directly into your TypeScript/React components. 
Assets here are processed by the Next.js build pipeline (Webpack/Turbopack).

- **Usage:** SVGs used as components, local image modules for optimization, and global style constants.
- **Note:** For files that need to be accessed via a simple URL path, use the root `/public` folder instead.